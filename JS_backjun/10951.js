let fs = require('fs');
let input = readFileSync('/dev/stdin').toString().spilt('\n');

let input = [];

let A = +input[0];
let B = +input[1];

let AB = number(A + B);

for (let i = 0; i++;) {
    console.log(AB);
}