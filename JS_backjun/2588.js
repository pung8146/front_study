// vs 코드에서먹히게 작성법

const fs = require('fs');
const stdin = (process.platform === 'linux'
  ? fs.readFileSync('/dev/stdin').toString()
  : `472
  385
`
).split('\n');

const abc = Number(input[a,b,c]);
const def = Number(input[d,e,f]);

// 나의풀이

const fs = require('fs');
const input = fs.readFileSync('/dev/stidn').toString().split=(' ');
const a = Number(input[a,b,c]);
const b = Number(input[d,e,f]);
console.log(Number((input[c]*input[d,e,f]);
console.log(Number((input[b]*input[d,e,f]);
console.log(Number((input[a]*input[d,e,f]);  

// 정답풀이
const fs = require('fs');
const input = fs.readFileSync('/dev/stdin').toString().split('\n');
const A = Number(input[0]);
const B = input[1];

const res3 = A * Number(B[2]);
const res4 = A * Number(B[1]);
const res5 = A * Number(B[0]);

console.log(res3);
console.log(res4);
console.log(res5);
console.log(A*Number(B));